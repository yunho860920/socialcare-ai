<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>업무용 챗봇 (Ollama)</title>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <style>
        :root { --bg-color: #121212; --chat-bg: #1e1e1e; --input-bg: #2d2d2d; --accent-color: #0052cc; --text-color: #e0e0e0; }
        body { font-family: 'Pretendard', sans-serif; background: var(--bg-color); color: var(--text-color); margin: 0; display: flex; flex-direction: column; height: 100vh; line-height: 1.6; }
        header { padding: 15px 20px; background: var(--chat-bg); border-bottom: 1px solid #333; display: flex; justify-content: space-between; align-items: center; }
        .title { font-weight: 700; font-size: 1.1rem; color: #fff; }
        #chat-box { flex: 1; overflow-y: auto; padding: 20px; display: flex; flex-direction: column; gap: 15px; }
        .message { padding: 12px 18px; border-radius: 12px; max-width: 85%; word-break: break-word; font-size: 0.95rem; }
        .ai { background: #2d2d2d; border-left: 3px solid #4facfe; }
        .user { background: var(--accent-color); color: white; align-self: flex-end; }
        .system { background: #263238; color: #b0bec5; text-align: center; align-self: center; font-size: 0.85rem; padding: 8px 15px; border-radius: 20px; }
        #input-area { padding: 20px; background: var(--chat-bg); border-top: 1px solid #333; display: flex; gap: 10px; }
        input { flex: 1; padding: 15px; border-radius: 10px; border: 1px solid #444; background: var(--input-bg); color: white; outline: none; }
        button { padding: 0 25px; border-radius: 10px; border: none; background: var(--accent-color); color: white; font-weight: bold; cursor: pointer; }
    </style>
</head>
<body>

<header>
    <div class="title">🦙 업무용 챗봇 (Ollama Local)</div>
    <div style="font-size: 0.8rem; color: #888;">연결 상태: <span id="status">확인 중...</span></div>
</header>

<div id="chat-box"></div>

<div id="input-area">
    <input type="text" id="user-input" placeholder="Ollama 연결 확인 중..." disabled>
    <button id="btn-send" disabled>전송</button>
</div>

<script>
    const OLLAMA_API_URL = "http://localhost:11434/api/chat";
    // [선택] 8GB 램에서 llama3.1이 너무 느리면 'llama3.2'로 바꾸세요.
    const MODEL_NAME = "llama3.1"; 

    let manualContent = "";
    // 대화 맥락 기억용 배열
    let chatHistory = []; 

    const chatBox = document.getElementById('chat-box');
    const userInput = document.getElementById('user-input');
    const sendBtn = document.getElementById('btn-send');
    const statusSpan = document.getElementById('status');

    function addMessage(type, text) {
        const div = document.createElement('div');
        div.className = `message ${type}`;
        div.innerHTML = type === 'ai' ? marked.parse(text) : text;
        chatBox.appendChild(div);
        chatBox.scrollTop = chatBox.scrollHeight;
    }

    // 1. 초기화: 매뉴얼 읽기 + Ollama 연결 확인
    async function initialize() {
        try {
            // 매뉴얼 로드
            const res = await fetch('manual.txt?v=' + Date.now());
            if(!res.ok) throw new Error("manual.txt 없음");
            manualContent = await res.text();
            addMessage("system", "✅ 매뉴얼 로드 완료");

            // Ollama 연결 테스트 (빈 요청 보내보기)
            try {
                const test = await fetch('http://localhost:11434/api/tags');
                if(test.ok) {
                    statusSpan.textContent = "🟢 연결됨 (" + MODEL_NAME + ")";
                    statusSpan.style.color = "#4caf50";
                    addMessage("system", "🚀 Ollama 엔진이 준비되었습니다.");
                    userInput.disabled = false;
                    sendBtn.disabled = false;
                    userInput.placeholder = "질문을 입력하세요...";
                    userInput.focus();
                } else { throw new Error("응답 없음"); }
            } catch(err) {
                throw new Error("Ollama가 실행되지 않았거나 접속이 차단됨 (2단계 설정 확인 필수)");
            }

            // 시스템 프롬프트 설정 (기억의 첫 단추)
            chatHistory.push({
                role: "system",
                content: `너는 아동보호전문기관의 베테랑 사회복지사야. 
아래 [매뉴얼]을 숙지하고, 사용자의 질문에 대해 한국어로 명확하고 친절하게 답변해. 
없는 내용은 지어내지 말고, 매뉴얼에 근거해서만 말해.

[매뉴얼 데이터]
${manualContent.substring(0, 20000)}` 
            });

        } catch (e) {
            statusSpan.textContent = "🔴 연결 실패";
            statusSpan.style.color = "#f44336";
            addMessage("system", `🚨 오류: ${e.message}<br>팁: 2단계 환경변수 설정 후 Ollama를 재시작했나요?`);
        }
    }

    async function sendMessage() {
        const text = userInput.value.trim();
        if (!text) return;

        addMessage("user", text);
        userInput.value = "";
        userInput.disabled = true;
        sendBtn.disabled = true;
        sendBtn.textContent = "생성 중...";

        // 사용자 질문을 기억에 추가
        chatHistory.push({ role: "user", content: text });

        try {
            const response = await fetch(OLLAMA_API_URL, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                    model: MODEL_NAME,
                    messages: chatHistory, // 지금까지의 대화 기록 전송
                    stream: true, // 타자기 효과
                    options: {
                        temperature: 0.1, // 차분하고 정확하게
                        num_ctx: 4096     // 문맥 기억 용량
                    }
                })
            });

            const reader = response.body.getReader();
            const decoder = new TextDecoder();
            let aiMsg = "";
            
            const aiDiv = document.createElement('div');
            aiDiv.className = 'message ai';
            chatBox.appendChild(aiDiv);

            while (true) {
                const { done, value } = await reader.read();
                if (done) break;
                
                const chunk = decoder.decode(value, { stream: true });
                // Ollama는 JSON 객체를 줄바꿈으로 구분해서 줍니다.
                const lines = chunk.split('\n');
                
                for (const line of lines) {
                    if (!line) continue;
                    try {
                        const json = JSON.parse(line);
                        const content = json.message?.content || "";
                        aiMsg += content;
                        aiDiv.innerHTML = marked.parse(aiMsg);
                        chatBox.scrollTop = chatBox.scrollHeight;
                    } catch (e) { console.error(e); }
                }
            }

            // 답변이 끝나면 기억에 추가
            chatHistory.push({ role: "assistant", content: aiMsg });

        } catch (e) {
            addMessage("system", "답변 생성 실패: " + e.message);
        } finally {
            userInput.disabled = false;
            sendBtn.disabled = false;
            sendBtn.textContent = "전송";
            userInput.focus();
        }
    }

    sendBtn.onclick = sendMessage;
    userInput.onkeypress = (e) => { if(e.key === 'Enter') sendMessage(); };
    
    initialize();
</script>
</body>
</html>
